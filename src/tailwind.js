const THEME_COLORS = require('./colors');

module.exports = {
  theme: {
    fontFamily: {
      display: ['Playfair Display', 'serif'],
    },
    extend: {
      colors: THEME_COLORS.lightTheme,
    },
  },
  variants: {},
  plugins: [],
};
